# 博客文章编写规范

本文件适用于整个仓库。新增或修改博客文章时，除用户的明确要求外，应遵循以下规范。

## 项目约定

- 博客正文位于 `content/posts/<年份>/<月份>/`，文件使用小写英文 kebab-case 命名，例如 `go-jws.md`。
- 文件路径会影响文章 URL。重命名文章前先确认旧 URL 是否已经发布；已发布文章如需改名，应同时考虑重定向和外部链接失效问题。
- 文章组件的完整文档和渲染示例位于 `content/previews/example.md`。使用 MDC 组件前必须先查阅该文件，并以其中的现有语法为准，不凭记忆猜测组件属性或插槽。
- Frontmatter 字段以 `content.config.ts` 中的 Schema 为准，分类以 `blog.config.ts` 的 `article.categories` 为准。
- 站点语言为简体中文，时区为 `Asia/Shanghai`。

## 写作前先核对事实

技术文章应以可验证的代码、命令输出、官方文档或实际排障过程为依据。

1. 阅读用户指定的源码、README、依赖文件和相关配置，先梳理真实执行链路。
2. 能安全运行的示例应实际运行，记录必要的工作目录、依赖版本、输入和输出。
3. 区分以下三类内容：
   - 源码当前实际实现；
   - 为便于讲解而简化或重命名的示例；
   - 面向生产环境的改进建议。
4. 不要把 README、注释或用户描述中的结论直接视为事实。发现概念错误时，应在文章中自然纠正并解释原因。
5. 不编造命令输出、性能数据、版本行为或不存在的 API。无法验证的内容应明确写出适用条件或不确定性。
6. 优先引用标准、项目官方文档和库的 API 文档。参考链接应指向与正文实际使用版本相符的资料。
7. 不泄露或复制真实密码、Token、私钥、Cookie、内部域名及其他敏感信息。示例值必须明显是虚构值。

## Frontmatter 规范

文章至少包含以下字段：

```yaml
---
title: 清晰、具体的文章标题
description: 用一到两句话说明问题、技术方案和读者能获得什么。
date: YYYY-MM-DD
updated: YYYY-MM-DD
categories:
  - Go语言
tags:
  - Go
  - 示例标签
---
```

- `title` 应包含核心技术和解决的问题，避免“学习笔记”“简单记录”等信息量过低的标题。
- `description` 应可独立作为搜索摘要阅读，不要重复标题，也不要写空泛的“本文介绍……”。
- 新文章的 `date` 和 `updated` 使用当天日期；修改正文后更新 `updated`，不要无故修改原始 `date`。
- `categories` 选择一个最匹配的现有分类。需要新增分类时，先在 `blog.config.ts` 中补充配置，避免产生无图标的孤立分类。
- `tags` 使用项目中常见的技术名称和正确大小写，数量以能准确检索文章为准，不堆砌同义标签。
- 完成并可发布的文章不设置 `draft: true`。
- 只有确有封面素材时才设置 `image`，不要留下占位 URL 或生成器注释。
- 不随意添加未被 `content.config.ts` 支持的 Frontmatter 字段；确有需求时先确认站点能够消费该字段。

## 正文结构

技术教程通常按下面的顺序组织，可根据主题删减，不要求机械套用：

1. **背景与目标**：说明要解决的问题、使用场景和最终结果。
2. **核心概念**：解释读懂实现所必需的术语，并澄清容易混淆的概念。
3. **环境与依赖**：列出关键版本、目录结构和运行前提。
4. **实现过程**：按照数据流或操作顺序拆解代码，解释为什么这样做。
5. **运行与验证**：给出可复现命令、关键输出以及成功判断标准。
6. **错误与边界**：说明常见失败、相对路径、版本差异、安全边界等问题。
7. **生产建议**：把最小示例与实际生产要求分开说明。
8. **总结与参考资料**：收束核心结论，提供权威资料和源码入口。

写作时还应遵循以下原则：

- 开头尽快告诉读者“解决什么问题”和“文章会完成什么”，不要用大段泛泛背景拖延进入主题。
- 一个章节只承担一个主要任务；标题应能概括结论，而不只是“介绍”“代码”“其他”。
- 先解释动机和数据流，再展示实现细节。避免连续堆放多个代码块而没有过渡说明。
- 首次出现的英文缩写应给出全称或中文含义，后文保持术语和大小写一致。
- 明确区分编码、哈希、签名和加密等相近概念，不使用“加密”笼统代替所有密码学操作。
- 核心结论直接写在正文中，不要只藏在折叠区域、选项卡或图片中。
- 删除与主题无关的发散内容。扩展知识应帮助读者理解当前实现，而不是变成另一篇文章。
- 结尾不要重复整篇正文，应浓缩为几条能够指导实践的结论。

## 代码和命令示例

- 代码应与文章描述及实际依赖版本一致。引用项目代码时，尽量保留真实函数名和调用方式。
- 为了可读性而改写源码时，应保证语义等价；存在实质差异时要明确说明“下面是简化版本”或“这里补充了错误处理”。
- 每个代码块前说明它解决什么问题，代码块后解释关键语句和预期结果。
- 代码块指定正确的语言。适合时添加文件名，提高上下文辨识度：

  ````md
  ```go [main.go]
  package main
  ```
  ````

- 命令应注明执行目录。相对路径依赖当前工作目录时必须指出，否则读者很容易得到 `no such file or directory`。
- 不保留无关的终端提示符、用户名和机器路径。只展示读者需要复制的命令及用于判断结果的输出。
- 输出包含 UUID、时间戳、签名或随机值时，说明每次运行会不同，并用省略号缩短无关的长内容。
- 长代码优先拆成有意义的小段；只有必须整体阅读时才使用代码块的 `expand`，不要滥用。
- 单行、需要直接复制的命令可以使用 `:copy`，普通命令组使用 `bash` 代码块。

## 使用组件增强可读性

组件用于表达信息结构，不是装饰。先把正文逻辑写清楚，再从 `content/previews/example.md` 选择能够降低理解成本的组件。

### 推荐组件与适用场景

| 组件 | 适合表达 | 使用要求 |
|---|---|---|
| `Alert` | 安全警告、版本差异、重要前提、阅读目标 | 一条只强调一个重点；普通说明不要全部做成 Alert |
| `LinkCard` | 源码仓库、官方文档、配套项目入口 | 标题和描述应说明链接价值；不要用作普通行内引用 |
| `Timeline` | 三步以上的签名流程、请求链路、安装步骤、状态变化 | 节点按时间或因果排序；每个节点保持简短 |
| `Tab` | 同一问题的并列方案、不同平台命令、签名/加密形态对比 | 各选项必须互斥或并列；切换后仍要有明确上下文 |
| `Folding` | Claims 词典、完整输出、兼容性细节等补充资料 | 关键步骤和结论不得只放在折叠内容里 |
| `CardList` | 生产检查项、概念速记、有明确边界的要点组 | 列表项应平行、简短；长段落改用普通章节 |
| `Copy` | 单条常用命令、URL 或配置值 | 仅用于确实需要一键复制的短内容 |
| `Pic` | 截图、架构图、无法用文字准确表达的视觉结果 | 使用真实且稳定的素材，提供准确的 caption |
| `Quote` | 标准原文或值得单独强调的定义 | 不用来包装作者自己的普通段落 |
| Markdown 表格 | 多个对象在相同维度上的精确比较 | 列数适中；移动端过宽时精简维度或拆表 |

可以使用 `Badge`、`Tip` 等行内组件提供少量辅助信息，但不要让一段话出现过多视觉元素。`Chat`、`Poetry`、`Blur`、`EmojiClock`、`VideoEmbed` 等组件通常不适合技术教程，除非内容本身确实需要这种表达方式。

### 组件选择规则

- 三个以上有明确先后或因果关系的步骤，优先考虑 `Timeline`。
- 三个以上对象需要按相同字段比较，优先使用表格。
- 多种等价方案占用篇幅较长，且读者通常只需要其中一种时，使用 `Tab`。
- 内容有参考价值但会中断主线时，使用 `Folding`。
- 涉及密钥、数据删除、兼容性或容易误用的前提时，使用合适类型的 `Alert`。
- 一个普通列表已经足够清楚时，不要强行替换为组件。
- 不连续堆叠多个大型组件。组件前后应有正文承接，说明读者为什么要看它以及看完得到什么。
- 同一信息不要同时用表格、卡片和普通列表重复表达。
- 不为了“页面更丰富”加入占位图片、无关图标、动画或外部随机素材。

### MDC 编写和验证

- 从 `content/previews/example.md` 复制最接近的现有语法，再替换内容。
- 严格检查 `::`、`#tab1`、命名插槽和嵌套缩进是否闭合。复杂组件嵌套时尤其注意缩进。
- 组件属性中包含数组、引号或特殊字符时，遵循示例中的 YAML 或属性写法。
- 不深层嵌套组件。一般最多一层；如果必须嵌套，预览确认移动端和桌面端都能阅读。
- 使用 `Tab` 或 `Folding` 后必须实际点击测试，不能只确认首屏渲染。
- 检查页面正文中是否意外显示 `#tab1`、`::timeline` 等 MDC 源码；出现即说明语法解析失败。
- 检查水平溢出。表格、长 Token、目录树和代码块尤其容易撑宽页面。

## 图片和外部资源

- 只有图片能显著降低理解成本时才添加图片；简单流程优先使用 `Timeline`、表格或文本图。
- 博客静态图片放在 `public/blog/`，使用稳定、语义明确的英文文件名，并在 Markdown 中使用 `/blog/<文件名>` 引用。
- 图片应有准确的替代文本或 `Pic` caption，不使用“图片”“截图”等无信息描述。
- 不直接依赖临时图床、随机图片服务或需要登录才能查看的素材。
- 引用第三方图片时确认授权和稳定性，必要时在正文或参考资料中标注来源。

## 语言与排版

- 使用简洁、自然的中文，避免机械翻译腔和没有信息量的套话。
- 中文与英文、数字之间保持项目现有排版风格；专有名词使用官方写法，如 `OpenSSL`、`Base64URL`、`gRPC`。
- 中文标点用于中文句子，代码标识符、路径、命令和字段名使用反引号包裹。
- 列表项保持语法结构平行。步骤使用有序列表，要点使用无序列表。
- 标题层级连续，从 `##` 开始正文一级章节，不跳级。
- 避免超过四级标题；内容过深时重新组织章节或使用折叠补充。
- 外部链接使用可读的描述文字；参考资料列表可以直接展示权威文档链接。

## 完成前的质量检查

文章完成后至少执行以下检查：

```bash
git diff --check
pnpm exec cspell content/posts/<年份>/<月份>/<文章>.md
pnpm build
```

如果开发服务器已启动，还应打开真实文章路由进行预览：

```bash
pnpm dev
```

预览时检查：

- 标题、描述、日期、分类和标签是否正确显示；
- 所有标题是否出现在目录中，层级是否合理；
- 代码高亮、文件名、换行和折叠是否正确；
- Alert、Timeline、Tab、Folding、LinkCard 等组件是否正确渲染；
- Tab 和 Folding 等交互组件是否能够操作；
- 是否存在 MDC 源码外露、断裂布局、水平溢出或失效链接；
- 图片是否加载成功，移动端宽度下是否仍可阅读；
- 浏览器控制台是否出现由本篇文章新增内容触发的错误。

构建输出中的既有警告可以记录但不要误报为本次文章错误；如果新增内容引入新的解析、构建或运行时错误，必须先修复再交付。

## 修改范围与交付

- 写文章时只修改与该文章直接相关的文件。不要顺手重写无关旧文章或调整站点组件样式。
- 如果为了文章新增分类、组件或图片，应在交付说明中列出这些额外改动。
- 不擅自提交、推送或发布文章，除非用户明确要求。
- 最终说明应包含文章文件路径、预览 URL、主要内容变化和已执行的检查。
